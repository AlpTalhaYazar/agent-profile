# Project Backend Instructions

This project uses PostgreSQL. Additional guidelines:

- Always use parameterised queries — never interpolate user input
- Use database transactions for multi-step operations
- Log SQL errors with correlation IDs
