version: 1
authProfiles:
  work:
    displayName: "Work (Acme Inc.)"
    anthropic:
      mode: apiKey
      secretRef: keyring://anthropic/work
    mcpSecretRefs:
      pg.user: keyring://pg/user
      pg.pw: keyring://pg/pw
      github.pat: keyring://github/pat
